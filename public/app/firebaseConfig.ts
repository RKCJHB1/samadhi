// Firebase support has been removed.
// These exports prevent import errors in any remaining files that might reference them,
// though all references should have been removed.

export const db = null;
export const storage = null;
